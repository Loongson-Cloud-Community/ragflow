"use strict";

var __assign = void 0 && (void 0).__assign || function () {
  __assign = Object.assign || function (t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];

      for (var p in s) {
        if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
      }
    }

    return t;
  };

  return __assign.apply(this, arguments);
};

var __awaiter = void 0 && (void 0).__awaiter || function (thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function (resolve) {
      resolve(value);
    });
  }

  return new (P || (P = Promise))(function (resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }

    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }

    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }

    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};

var __generator = void 0 && (void 0).__generator || function (thisArg, body) {
  var _ = {
    label: 0,
    sent: function sent() {
      if (t[0] & 1) throw t[1];
      return t[1];
    },
    trys: [],
    ops: []
  },
      f,
      y,
      t,
      g;
  return g = {
    next: verb(0),
    "throw": verb(1),
    "return": verb(2)
  }, typeof Symbol === "function" && (g[Symbol.iterator] = function () {
    return this;
  }), g;

  function verb(n) {
    return function (v) {
      return step([n, v]);
    };
  }

  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");

    while (_) {
      try {
        if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
        if (y = 0, t) op = [op[0] & 2, t.value];

        switch (op[0]) {
          case 0:
          case 1:
            t = op;
            break;

          case 4:
            _.label++;
            return {
              value: op[1],
              done: false
            };

          case 5:
            _.label++;
            y = op[1];
            op = [0];
            continue;

          case 7:
            op = _.ops.pop();

            _.trys.pop();

            continue;

          default:
            if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
              _ = 0;
              continue;
            }

            if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
              _.label = op[1];
              break;
            }

            if (op[0] === 6 && _.label < t[1]) {
              _.label = t[1];
              t = op;
              break;
            }

            if (t && _.label < t[2]) {
              _.label = t[2];

              _.ops.push(op);

              break;
            }

            if (t[2]) _.ops.pop();

            _.trys.pop();

            continue;
        }

        op = body.call(thisArg, _);
      } catch (e) {
        op = [6, e];
        y = 0;
      } finally {
        f = t = 0;
      }
    }

    if (op[0] & 5) throw op[1];
    return {
      value: op[0] ? op[1] : void 0,
      done: true
    };
  }
};

var __importDefault = void 0 && (void 0).__importDefault || function (mod) {
  return mod && mod.__esModule ? mod : {
    "default": mod
  };
};

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ASNFilesGenerator = exports.SVGFilesReader = void 0;
/**
 * @file ASN 文件的生成
 */

var fs_1 = require("fs");

var path_1 = require("path");

var globby_1 = __importDefault(require("globby"));

var chalk_1 = __importDefault(require("chalk"));

var utils_1 = require("./utils");

var core_1 = require("./core");
/**
 * SVG 文件读取器
 */


function SVGFilesReader(_a) {
  var entry = _a.entry;
  return __awaiter(this, void 0, void 0, function () {
    var pattern, SVGFilepaths;
    return __generator(this, function (_b) {
      if (!(0, utils_1.existing)(entry)) {
        return [2
        /*return*/
        , Promise.reject(new Error(entry + " is not existing."))];
      }

      pattern = (0, path_1.resolve)(entry, "**", "*.svg").replace(/\\/g, "/");
      SVGFilepaths = globby_1.default.sync(pattern);
      return [2
      /*return*/
      , SVGFilepaths.map(function (SVGFile) {
        var SVGContent = (0, fs_1.readFileSync)(SVGFile, "utf-8");
        return {
          filepath: SVGFile,
          content: SVGContent
        };
      })];
    });
  });
}

exports.SVGFilesReader = SVGFilesReader;
/**
 * ASN 文件生成
 */

function ASNFilesGenerator(_a) {
  var entry = _a.entry,
      output = _a.output,
      ASNDirName = _a.ASNDirName,
      typescript = _a.typescript,
      verbose = _a.verbose;
  return __awaiter(this, void 0, void 0, function () {
    var SVGFiles, ASNOutput, ASNEntryFile, ASNNodes, ASNOutputDir, i, _b, filename, content, asnFilepath;

    return __generator(this, function (_c) {
      switch (_c.label) {
        case 0:
          return [4
          /*yield*/
          , SVGFilesReader({
            entry: entry
          })];

        case 1:
          SVGFiles = _c.sent();

          if (verbose) {
            console.log();
            console.log(chalk_1.default.greenBright("[ASNFilesGenerator]SVG files"), SVGFiles);
            console.log();
          }

          return [4
          /*yield*/
          , (0, core_1.ANSOutputTransformer)({
            SVGFiles: SVGFiles,
            ASNDirName: ASNDirName,
            typescript: typescript,
            verbose: verbose
          })];

        case 2:
          ASNOutput = _c.sent();
          ASNEntryFile = ASNOutput.entryFile, ASNNodes = ASNOutput.ASNNodes;
          ASNOutputDir = (0, path_1.resolve)(output, ASNDirName || "asn");
          (0, utils_1.ensure)(ASNOutputDir); // generate index.ts

          (0, fs_1.writeFileSync)((0, path_1.resolve)(output, ASNEntryFile.filename), ASNEntryFile.content); // generate asn files

          for (i = 0; i < ASNNodes.length; i += 1) {
            _b = ASNNodes[i], filename = _b.filename, content = _b.content;
            asnFilepath = (0, path_1.resolve)(ASNOutputDir, filename);

            if (verbose) {
              console.log(chalk_1.default.gray("[ASNFilesGenerator]write ASN file"), asnFilepath);
            }

            (0, fs_1.writeFileSync)(asnFilepath, content);
          } // copy types.ts


          if (typescript) {
            (0, utils_1.generateTypeFiles)({
              output: output
            });
          }

          return [2
          /*return*/
          , __assign(__assign({}, ASNOutput), {
            output: ASNOutputDir
          })];
      }
    });
  });
}

exports.ASNFilesGenerator = ASNFilesGenerator;