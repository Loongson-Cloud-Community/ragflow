interface ReactIconsGeneratorOptions {
    /**
     * SVG 目录
     */
    /**
     * react icon 入口文件
     */
    entryFile: {
        filename: string;
        content: string;
    };
    /**
     * react icon 列表
     */
    icons: {
        filename: string;
        identifier: string;
        content: string;
    }[];
    /**
     * react icons 输出目录
     */
    output: string;
    /**
     * icons 文件夹名称
     */
    iconsDirName?: string;
    /**
     * 生成 ts
     */
    typescript?: boolean;
    /**
     * 输出更多信息
     */
    verbose?: boolean;
}
/**
 * react icons 生成器
 */
export declare function reactIconsGenerator({ entryFile, icons, output, iconsDirName, typescript, verbose, }: ReactIconsGeneratorOptions): Promise<{
    id: string;
    identifier: string;
}[]>;
/**
 * 从 SVG 目录生成 react icons
 */
export declare function reactIconsGeneratorFromSVGDir({ entry, output, ASNOutputDir, iconsOutputDir, iconsDirName, ASNFilepath, typescript, verbose, }: {
    entry: string;
    output?: string;
    ASNOutputDir?: string;
    iconsOutputDir?: string;
    iconsDirName?: string;
    ASNFilepath?: string;
    typescript?: boolean;
    verbose?: boolean;
}): Promise<{
    id: string;
    identifier: string;
}[]>;
interface SingleReactIconGeneratorOptions {
    SVGPath: string;
    output: string;
    ASNDirName?: string;
    iconsDirName?: string;
    typescript?: boolean;
}
/**
 * 单个 icon 组件生成
 * 为了增量生成 icon
 */
export declare function singleReactIconGenerator({ SVGPath, output, ASNDirName, iconsDirName, typescript, }: SingleReactIconGeneratorOptions): Promise<import("./core").ASNNode>;
export {};
